import {
  FETCH_MOST_POPULAR_TV_TMDB,
  FETCH_NEW_TV_ON_NETFLIX
} from "../actions/types";

export default function(state = {}, action) {
  switch (action.type) {
    default:
      return {
        ...state
      };
  }
}
